name: Deploy imeon-mqtt

on:
  push:
    branches: [ "main" ] 
  workflow_dispatch:

jobs:
  deploy:
    name: "Perkelimasi i hera"
    runs-on: [self-hosted]

    steps:
    - uses: actions/checkout@v3
    
